import 'package:flutter/widgets.dart';

class AppColors {
  /// Spotify's signature green
  static const Color spotifyGreen = Color(0xFF1DB954);

  /// Dark background (almost black)
  static const Color backgroundDark = Color(0xFF121212);

  /// Darker gray (for cards, surfaces)
  static const Color darkGray = Color(0xFF121212);

  /// Light gray (for text or icons inactive)
  static const Color lightGray = Color(0xFFB3B3B3);

  /// White
  static const Color white = Color(0xFFFFFFFF);

  /// Black
  static const Color black = Color(0xFF000000);

  /// Error Red (optional custom)
  static const Color error = Color(0xFFe22134);

  /// Surface (e.g. card background)
  static const Color surface = Color(0xFF282828);

  /// Border stroke (optional)
  static const Color stroke = Color(0xFF404040);

  /// Navigation inactive icon
  static const Color navInactive = Color(0xFFB3B3B3);

  /// Navigation active icon (use white or green depending on design)
  static const Color navActive = Color(0xFFFFFFFF);
}
